# VoiceBank-DEMAND (local export)

Not committed (too large). Prepare with:

```powershell
# HF dataset already at ../../data/datasets/VoiceBank-DEMAND-16k (also not in git)
python scripts/prepare_voicebank.py
```

Or download: https://huggingface.co/datasets/JacobLinCool/VoiceBank-DEMAND-16k
